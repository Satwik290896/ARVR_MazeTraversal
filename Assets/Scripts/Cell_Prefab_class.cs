using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cell_Prefab_class : MonoBehaviour
{
    // Start is called before the first frame update
    public class cellAssign
    {
        bool isActive;
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
